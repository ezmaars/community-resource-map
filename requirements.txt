# Core framework
Django>=5.0,<5.3

# Environment / settings management
django-environ>=0.11,<0.13

# Production WSGI server
gunicorn>=21.2,<24.0

# Static file serving without a separate web server
whitenoise>=6.6,<7.1

# PostgreSQL driver (psycopg 3). Not needed for SQLite-only local dev,
# but harmless to install and required for the Docker/Postgres setup.
psycopg[binary]>=3.1,<3.3
